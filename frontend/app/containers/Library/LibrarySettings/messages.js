/*
 * LibrarySettings Messages
 *
 * This contains all the text for the LibrarySettings container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LibrarySettings';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LibrarySettings container!',
  },
});
