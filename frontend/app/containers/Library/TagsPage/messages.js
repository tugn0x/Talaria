/*
 * ReferencesLabels Messages
 *
 * 
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LibraryTags';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Tags',
  }, 
});
