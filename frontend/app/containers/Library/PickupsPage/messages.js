/*
 * References Form
 *
 * This contains all the text for the HomePage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PickupsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Pickup list',
  },    
});

