/*
 * Messages
 *
 * This contains all the text for the container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssociateLibraryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Choose a library',
  },  
});
