/*
 * VerificationForm Messages
 *
 * This contains all the text for the VerificationForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.VerificationForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VerificationForm component!',
  },
});
